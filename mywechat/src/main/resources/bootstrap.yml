server:
  port: 8004
spring:
  application:
    name: mywechat
  cloud:
    config:
      discovery:
        enabled: true
        service-id: service-config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/