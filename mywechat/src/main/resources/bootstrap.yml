server:
  port: 8003
spring:
  application:
    name: oauth-server
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: service-config
#      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

management:
  endpoint:
    health:
      show-details: always