server:
  port: 8002

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

spring:
  application:
    name: service-gateway
  cloud:
      config:
        discovery:
          enabled: true
          service-id: service-config
        profile: dev
