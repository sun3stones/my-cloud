server:
  port: 8003
spring:
  application:
    name: service-oauth
  cloud:
    config:
      discovery:
        enabled: true
        service-id: service-config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/